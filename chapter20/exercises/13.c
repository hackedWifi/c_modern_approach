/* if n is an unsigned integer variable, what effect does the following statement 
 * have on the bits in n?
 *
 * n &= n - 1;
 *
 * Hint: Consider the effect on n if this statement is executed more than once.
 */

// It sets the right most significant bit to 0 
