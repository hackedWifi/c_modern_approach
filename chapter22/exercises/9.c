/* Which one of the following calls is not valid way or reading one character
 * from the standard input stream?
 *
 * (a) getch()
 * (b) getchar()
 * (c) getc(stdin)
 * (d) fgetc(stdin)
 *
 *
 */

// (a) is that a valid I/O C function
