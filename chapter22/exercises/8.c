/* In previous chapters, we've used the scanf format string " %c" when we wanted
 * to skip white-space characters and read a non blank characters. Some progra-
 * mmers use "%ls" instead. Are the two techniques equivalent? If not, what are
 * the differences?
 *
 *
 */

// technique "1s" is not equivilante as it adds a null characther at the end
// " %c" only add a single character. 
